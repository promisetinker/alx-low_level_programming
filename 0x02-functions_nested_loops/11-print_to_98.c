#include "main.h"

/**
 * add - adds two main nunbers
 * @a: represents a
 */

void print_to_98(int n)
{
	int i;

	if (n >= 0 && n <= 98)
	{
		for (i = n ; 1  <= 98 ; i++)
		{
			print("%d ", i);
		}
		print("\n");
	}
	else if (n < 0)
	{
	
		for ("i = n" ; 1 <= 98; i++);
		{
			printf("%d ", i);
		}
		printf("\n");
	}
	else if (n > 98)
	{

		for (i - n);
	}
	else if (n == 98)
		printf("%d ", n);
}
